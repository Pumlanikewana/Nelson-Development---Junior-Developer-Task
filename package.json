{
  "name": "junior-dev-task",
  "version": "1.0.0",
  "description": "Junior Developer Task - String Sorting API",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": ">=14"
  }
}